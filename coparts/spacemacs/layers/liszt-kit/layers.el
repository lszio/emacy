(configuration-layer/declare-layers
 '(;; -- editing --
   multiple-cursors
   ;; -- switch --
   ibuffer
   ;; -- display --
   eaf
   treemacs
   better-defaults
   ;; -- reading --
   elfeed
   ;; -- input --
   (chinese)))
